# Sprint 3

## User Story:
Security Office Staff can close reports when items are resolved or when processing is finished without resolution

| Test Case | Input | Expected Output | Actual output |
|---|---|---|---|
| 1 (Failure  Fixed) | **Precondition:** `urls.py` includes routes for `resolve_report` and `close_report`, but `app/views.py` does not yet contain the new functions. 1. Start the server 2. Open a report action URL that uses `resolve_report` | The application should load the route without view import errors | Failed initially. Error: `module 'app.views' has no attribute 'resolve_report'`. **Fixed** by adding `resolve_report` and `close_report` functions into `app/views.py`. After fix, routes worked correctly. |
| 2 (Failure  Fixed) | **Precondition:** Staff status dropdown in `report_detail.html` is updated by paste/edit in MSVS. 1. Open report detail page 2. Render template with updated status options | The detail page should render without Django template parsing errors | Failed initially. Error: `Could not parse the remainder: '=' from 'report.status='`. Cause: MSVS auto-correct changed Django template syntax during paste. **Fixed** by correcting template conditions. After fix, page rendered successfully. |
| 3 (Success) | **Precondition:** Model updated to use two-dimensional design: `status` (`pending/approved/rejected`) and `outcome` (`open/resolved/closed`). Migration applied successfully. Existing reports have default outcome `open`. 1. Run `python manage.py makemigrations` 2. Run `python manage.py migrate` | Migration creates the new `outcome` field and existing reports remain accessible with default `open` outcome | Passed. Migration was created and applied successfully. Existing reports remained accessible and outcome support was added. |
| 4 (Success) | **Precondition:** Approved report exists with `outcome = open`. User is logged in as Security Office Staff. 1. Open report detail page 2. Click **Mark as Resolved** | The system sets `outcome = resolved`, redirects back to report detail page, and shows success feedback | Passed. Resolve action worked, report outcome changed to resolved, and success feedback was shown. |
| 5 (Success) | **Precondition:** Approved report exists with `outcome = open`. User is logged in as Security Office Staff. 1. Open report detail page 2. Click **Close Report** | The system sets `outcome = closed`, redirects back to report detail page, and shows success feedback | Passed. Close action worked, report outcome changed to closed, and success feedback was shown. |
| 6 (Success) | **Precondition:** Report detail page is opened for a report with `status = approved` and `outcome = open`. User is logged in as Security Office Staff. 1. Observe the staff control panel | Staff user sees review status dropdown and also sees **Mark as Resolved** and **Close Report** buttons | Passed. Buttons were visible only in the valid state `approved + open`. |
| 7 (Success) | **Precondition:** Report outcome has already been changed to `resolved` or `closed`. User is logged in as Security Office Staff. 1. Open report detail page 2. Observe finish-action area | Resolve/Close action buttons are no longer shown for final reports | Passed. After final outcome was set, Resolve and Close buttons disappeared as expected. |
| 8 (Success) | **Precondition:** Report detail page is opened after Stage 8 changes. 1. Open report detail page 2. Observe report metadata section | The page shows both review **Status** and lifecycle **Outcome** | Passed. Detail page displayed both Status and Outcome correctly. |
| 9 (Success) | **Precondition:** Report detail page is opened for staff after Stage 8 changes. 1. Open the staff status dropdown | Dropdown contains only review workflow values: `pending`, `approved`, `rejected` | Passed. Dropdown was corrected to contain only review status values and no longer included resolved/closed. |
| 10 (Success) | **Precondition:** User is the report owner. Report is unpublished. Outcome is not `open` (`resolved` or `closed`). 1. Open report detail page 2. Check whether **Edit Report** button is available | Owner cannot edit a report that has final outcome status | Passed. Edit button was hidden when outcome was not `open`, preventing further editing of finalized reports. |
| 11 (Success) | **Precondition:** Existing Stage 7 status-change flow remains available. User is logged in as Security Office Staff. 1. Open report detail page 2. Change status to `pending`, `approved`, or `rejected` 3. Submit the form | Review workflow continues to work without using outcome values | Passed. Status update flow continued to work correctly after Stage 8 redesign. |
| 12 (Success) | **Precondition:** Full Stage 8 implementation completed. 1. Start application 2. Open report detail page 3. Test resolve/close flow and status flow | Application runs without syntax, routing, model, migration, or template errors | Passed. Final implementation worked successfully. |
