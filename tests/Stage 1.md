# Sprint 3

## User Story 1: Campus Community User and Security Office Staff users can log in and log out, and the system recognizes role membership through Django groups

| Test Case | Input | Expected Output | Actual Output |
|---|---|---|---|
| **1 (Success) Login page opens** | **Precondition:** Application is running. 1. Open `/login/`. | The login page is displayed with username and password fields and a Log in button. | **Passed.** Login page rendered correctly. |
| **2 (Success) Valid Campus Community User login** | **Precondition:** User exists in Django admin and belongs to **Campus Community User** group. 1. Open `/login/`. 2. Enter valid campus user credentials. 3. Click **Log in**. | The system authenticates the user and redirects to the home page. The navbar changes to authenticated state. | **Passed.** User login worked and redirected correctly. |
| **3 (Success) Valid Security Office Staff login** | **Precondition:** User exists in Django admin and belongs to **Security Office Staff** group. 1. Open `/login/`. 2. Enter valid staff user credentials. 3. Click **Log in**. | The system authenticates the user and redirects to the home page. The navbar changes to authenticated state. | **Passed.** Staff user login worked and redirected correctly. |
| **4 (Failure) Invalid login credentials** | 1. Open `/login/`. 2. Enter incorrect username and/or password. 3. Click **Log in**. | The user is not authenticated. The login page remains open and an error message is shown. | **Passed.** Invalid login remained on the login page and showed the validation error message. |
| **5 (Success) Logged-in user cannot stay on login page** | **Precondition:** User is already logged in. 1. Open `/login/`. | The system redirects the authenticated user instead of showing the login form again. | **Passed.** Redirect behavior worked after enabling `redirect_authenticated_user=True`. |
| **6 (Success) Logout works from navbar** | **Precondition:** User is logged in. 1. Click **Log out / Log off** in the navbar. | The session is ended and the user is redirected to the home page. The navbar changes to anonymous state. | **Passed.** Logout worked and returned user to `/`. |
| **7 (Success) Anonymous user sees Log in option** | **Precondition:** User is not logged in. 1. Open home page. 2. Observe navbar. | Navbar shows **Log in** and does not show authenticated user options. | **Passed.** Anonymous navbar state worked correctly. |
| **8 (Success) Authenticated user sees username and logout option** | **Precondition:** User is logged in. 1. Open home page. 2. Observe navbar. | Navbar shows the logged-in username and logout option. | **Passed.** Authenticated navbar state worked correctly. |
| **9 (Success) Group roles created in Django admin** | **Precondition:** Superuser is available. 1. Open `/admin/`. 2. Go to **Groups**. 3. Create **Campus Community User**. 4. Create **Security Office Staff**. | Both groups are saved in Django admin and available for assignment to users. | **Passed.** Both groups were created successfully. |
| **10 (Success) Users assigned to exactly one role group** | **Precondition:** Groups already exist. 1. Open a user in Django admin. 2. Assign the user to one group. 3. Save the user. | The user is saved with the selected role group. | **Passed.** Users were created and assigned through Django admin successfully. |
| **11 (Success) Template role flags available for authenticated user** | **Precondition:** `user_roles` context processor is configured and user is logged in. 1. Load a page rendered with the main layout. 2. Access role-based template flags. | Templates can safely use `IS_CAMPUS_COMMUNITY_USER` and `IS_SECURITY_OFFICE_STAFF` without errors. | **Passed.** Context processor setup worked after updating `settings.py` and `app/context_processors.py`. |
| **12 (Success) Template role flags safe for anonymous user** | **Precondition:** No user is logged in. 1. Open a public page. | The page renders successfully and both role flags evaluate safely to `False`. | **Passed.** Public pages continued to load without crashes for anonymous users. |
| **13 (Success) Existing public pages remain accessible** | 1. Open `/`. 2. Open `/about/`. 3. Open `/contact/`. | All public pages load without authentication errors after Stage 1 changes. | **Passed.** Public pages remained accessible and stable. |
| **14 (Success) Footer/version label remains unchanged** | 1. Open any page using the main layout. 2. Check the footer. | Footer still displays the version/build information correctly. | **Passed.** Footer/version label remained intact after auth menu changes. |
