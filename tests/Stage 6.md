# Sprint 3

**User Story:** Campus Community User can edit own Lost Item report before approval

| Test Case | Input | Expected Output | Actual Output |
|---|---|---|---|
| 1 (Success) | **Precondition:** User is logged in and owns an unapproved Lost Item report created in admin.  1. Open `/reports/<lost_report_id>/edit/lost/`  2. Verify that the edit page opens | The system opens the Lost Item report edit page for the owner. | The route and view were implemented and tested. The edit page opened successfully for the owner. |
| 2 (Failure) | **Precondition:** User is logged in but does not own the Lost Item report.  1. Open `/reports/<lost_report_id>/edit/lost/` for another user�s report | The system prevents editing and redirects the user to the Reports page. | Permission restriction was tested during Stage 6. The user was redirected to `/reports/`. |
| 3 (Failure) | **Precondition:** User is logged in and owns a published Lost Item report.  1. Open `/reports/<lost_report_id>/edit/lost/` | The system prevents editing of the published report and redirects the user to the Reports page. | Publish restriction was tested during Stage 6. The user was redirected to `/reports/`. |
| 4 (Failure) | **Precondition:** User is logged in and opens the Lost edit URL for a Found Item report.  1. Open `/reports/<found_report_id>/edit/lost/` | The system prevents editing through the wrong route and redirects the user to the Reports page. | Report type restriction was tested during Stage 6. The user was redirected to `/reports/`. |
| 5 (Success) | **Precondition:** User is logged in and owns an unapproved Lost Item report.  1. Open `/reports/<lost_report_id>/edit/lost/`  2. Verify page heading | The page heading displays `Edit Lost Item Report`. | Initially, the shared template was Found-specific, so Lost edit text would not be correct. **Fixed.** After the template update, the page heading displayed `Edit Lost Item Report` correctly. |
| 6 (Success) | **Precondition:** User is logged in and owns an unapproved Lost Item report.  1. Open `/reports/<lost_report_id>/edit/lost/`  2. Verify primary button label | The primary button displays `Save Changes`. | Initially, the shared template was Found-specific, so the Lost edit button text would not be correct. **Fixed.** After the template update, the button displayed `Save Changes` correctly. |
| 7 (Success) | **Precondition:** User is logged in and owns an unapproved Lost Item report.  1. Open `/reports/<lost_report_id>/edit/lost/`  2. Change Title, Description, or Location  3. Click `Save Changes` | The system saves the updated report and redirects back to the edit page with `?updated=1`. | Save/update flow was tested during Stage 6. The report was saved successfully and the user was redirected back to the edit page. |
| 8 (Success) | **Precondition:** User is logged in and has just saved valid changes to own unapproved Lost Item report.  1. Verify the page after redirect | The page shows a success message indicating that the report was updated successfully. | Tested during Stage 6. The success message was shown correctly after update. |
| 9 (Failure) | **Precondition:** User is logged in and owns an unapproved Lost Item report.  1. Open `/reports/<lost_report_id>/edit/lost/`  2. Clear one required field  3. Click `Save Changes` | The system keeps the user on the edit page, shows validation errors, and does not save invalid data. | Validation behavior was tested during Stage 6. The form remained on the page with validation errors, and invalid data was not saved. |
| 10 (Success) | **Precondition:** User is logged in and views the detail page of own unapproved Lost Item report.  1. Open `/reports/<lost_report_id>/`  2. Verify the Edit button | The detail page shows the `Edit Report` button for the owner. | Initially, the detail page supported the Edit button only for Found reports. **Fixed.** After the template update, the `Edit Report` button appeared correctly for own unapproved Lost reports. |
| 11 (Failure) | **Precondition:** User is logged in but does not own the Lost Item report.  1. Open `/reports/<lost_report_id>/`  2. Verify the Edit button | The detail page does not show the `Edit Report` button. | Tested during Stage 6. The Edit button was not shown for a non-owner. |
| 12 (Failure) | **Precondition:** User is logged in and owns a published Lost Item report.  1. Open `/reports/<lost_report_id>/`  2. Verify the Edit button | The detail page does not show the `Edit Report` button for a published report. | Tested during Stage 6. The Edit button was not shown for a published Lost Item report. |
| 13 (Regression) | **Precondition:** Existing Found Item report edit flow is available.  1. Open Found Item report create page  2. Open Found Item report edit page  3. Save changes on Found Item report | Existing Found Item report create/edit functionality continues to work without regression. | Regression check was performed during Stage 6. Found create and Found edit continued to work correctly. |
