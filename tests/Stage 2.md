# Sprint 3

**User Story:** Campus Community User can register a new account with email and complete a placeholder verification-code step before account creation.

| Test Case | Input | Expected Output | Actual output |
|---|---|---|---|
| 1 (Success) | 1. Open `/register/` as a not logged user. 2. View the registration page. | Registration page opens successfully and displays fields for username, email, password, and password confirmation. | Initially failed with template error because `register.html` did not exist. **Fixed.** After creating `register.html`, `/register/` opened successfully and displayed the registration form. |
| 2 (Failure) | 1. Open `/register/verify/` directly without first submitting registration. | System should not allow direct access to verification step without pending registration data. User should be redirected back to `/register/`. | Worked as expected after route and view setup: direct access to `/register/verify/` without pending registration redirects to `/register/`. |
| 3 (Success) | 1. Open `/register/`. 2. Enter valid username, valid email, password, and matching password confirmation. 3. Click **Register**. | System stores temporary registration data in session and redirects user to verification code page. | Worked after `register.html` and `register_verify.html` were created: valid registration data redirected to `/register/verify/`. |
| 4 (Success) | 1. Complete Step 3. 2. Open the verification page. | Verification page opens and shows a code input field and the entered email address. | Worked as expected: verification page displayed the email and the code input field. |
| 5 (Success) | 1. Complete Step 3. 2. On verification page, enter any non-empty code. 3. Click **Confirm**. | Because this stage uses placeholder verification, system accepts the code, creates the Django user, assigns group **Campus Community User**, clears pending session data, and redirects to login page with `?registered=1`. | Worked as expected: entering a code completed the placeholder flow, created the user only after verification step, assigned **Campus Community User**, and redirected to `/login/?registered=1`. |
| 6 (Failure) | 1. Open `/register/`. 2. Enter an email already used by another account. 3. Submit the form. | Form should stay on registration page and show validation error that a user with this email already exists. | Implemented in `clean_email()` in Stage 2 form update. Validation rule added successfully. Expected UI behavior is now supported by the form logic. |
| 7 (Failure) | 1. Open `/register/`. 2. Enter mismatched password and password confirmation. 3. Submit the form. | Form should not submit successfully and should display password mismatch validation error. | Supported by Django `UserCreationForm` behavior after Stage 2 form implementation. Registration remains on the same page until corrected. |
| 8 (Success) | 1. Stay logged out. 2. Open site navbar. | Navbar should display **Register** and **Log in** links for guests. | Initially navbar showed only **Log in**. **Fixed.** After updating `loginpartial.html`, logged-out users see both **Register** and **Log in** links. |
| 9 (Success) | 1. Log in as an existing user. 2. Open site navbar. | Navbar should hide guest registration links and show authenticated user information with role-aware label and **Log out** link. | Worked as expected with current customized `loginpartial.html`: authenticated users see role label (`User:` or `Staff:`), username, and **Log out**; Register link is not shown. |
| 10 (Success) | 1. Start the server after Stage 2 changes. 2. Open existing pages `/`, `/about/`, `/contact/`, `/login/`. | Existing pages and login/logout functionality should continue to work with no regression. | Worked as expected after Step 2.3 and 2.4 testing: server started successfully, static pages still worked, and login/logout routing remained intact. |
| 11 (Failure) | 1. Add `/register/` and `/register/verify/` routes. 2. Open `/register/` before creating templates. | Route should resolve, but page render will fail until template exists. | Failed with `TemplateDoesNotExist` for `app/register.html`. **Fixed.** After creating `register.html` and `register_verify.html`, route and page rendering worked correctly. |
| 12 (Success) | 1. Review database/migration impact after Stage 2 implementation. | No new migrations should be required because no model schema was changed. | Confirmed in conversation: Stage 2 used existing Django `User` and `Group` tables only, so no migration was needed. |
